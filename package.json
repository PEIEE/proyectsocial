{
  "name": "chatklax",
  "version": "1.0.0",
  "scripts": {
    "build": "cp -r src/* public/ && cp node_modules/firebase/firebase*.js public/ && cp node_modules/peerjs/dist/peerjs.min.js public/",
    "start": "npx serve public"
  },
  "dependencies": {
    "firebase": "^10.14.1",
    "peerjs": "^1.5.2"
  }
}
